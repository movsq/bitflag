export type RegisterBody = {
    reqEmail: string;
    reqPassword: string;
}

// They are the same thing for now, but won't be later
export type LoginBody = {
    reqEmail: string;
    reqPassword: string;
}