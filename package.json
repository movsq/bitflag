{
    "name": "bitflag",
    "version": "1.0.0",
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "@types/node": "^25.6.0",
        "typescript": "^6.0.2"
    },
    "scripts": {
        "dev:server": "bun --watch packages/server/src/index.ts",
        "dev:client": "bun run --cwd packages/client dev"
    }
}